function Basic(){
    return(
        //Fragment "<>"  
        //Why this?
        //because return should be from one parent element
        //Every tag that is used in react must have a closing tag
        <> 
            <h2>Hello every one</h2>
            <p>let's start our react journey</p>
        </>
    );
}

//we have to export this file
export default Basic;